отложенная задача.

> Boot-тест показал ~133 script error от предметов, зовущих buildin'ы ep11-13 фич, которых в pre-12 uAthena нет
> Эти предметы грузятся (определение есть), но скрипт пустой + ошибка в лог при старте. Почти все — из data-backport (мой ранний 25f3be3) и относятся к out-of-era контенту для pre-12. Это противоречит твоей концепции «ep12-13 не тащим».
> Записал находку в чеклист (276f34c).
> ~133 предмета из data-backport зовут buildin'ы ep11-13 фич (rentitem/mercenary/setfont/…), которых в pre-12 uAthena нет — дают ошибку парсинга при старте. Как поступить?
> [14.06.2026 23:22] Vitaly MN: ❯ 1. Нейтрализовать скрипты
> Заменить сломанный скрипт этих предметов на пустой {} — предметы остаются (имя/статы), эффекта нет, ошибки при старте уходят. Один коммит, низкий риск

проведи анализ, что необходимо для бекпортирования?
renttime
setfont
mercenary_create/mercenary_sc_start
searchstore/buyingstore
bonusautoscript/bonusautoscript2

что из этого потребует новых функций в клиенте? нам необходимо понимать: будет ли поддерживать функционал текущий клиент; если необходимо обновлять клиент, то нам нужно понимать до какой версии, т.к. обновления клиента ограничено требованиями нашей концепции соблюдения старых механик.