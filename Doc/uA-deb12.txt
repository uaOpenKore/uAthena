1.

vi /etc/apt/sources.list
deb http://deb.debian.org/debian/ bookworm main non-free-firmware contrib
deb http://security.debian.org/debian-security bookworm-security main non-free-firmware
deb http://deb.debian.org/debian/ bookworm-updates main non-free-firmware
deb http://deb.debian.org/debian bookworm-backports main contrib non-free non-free-firmware
deb https://ftp.debian.org/debian/ bookworm-proposed-updates contrib main non-free non-free-firmware

apt -y install mc netselect-apt
netselect-apt
apt update
apt -y upgrade
apt -y install cockpit* qemu-kvm curl podman-compose git libncurses5-dev gcc make autoconf exuberant-ctags bc libssl-dev docker*  selinux-basics selinux-policy-default auditd ufw

vi /etc/selinux/config
SELINUX=disabled

vi /etc/sudoers
pix     ALL=(ALL:ALL) ALL

systemctl enable cockpit.socket podman

reboot


2.
~ Сorrect creation of swap:
dd if=/dev/zero of=/swapfile bs=1MB count=4096
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo "/swapfile swap swap defaults 0 0" >>/etc/fstab
echo "vm.swappiness = 40" >>/etc/sysctl.conf


3.
~ Custom build kernel
apt -y install build-essential libncurses-dev bison flex libssl-dev libelf-dev bc fakeroot dpkg-dev rsync dwarves debhelper-compat
git clone git://git.kernel.org/pub/scm/linux/kernel/git/stable/linux-stable.git
cd linux-stable
git tag -l | grep "v6.12\."
git checkout -b 6.12.1 v6.12.1
cp /boot/config-$(uname -r) .config
make menuconfig
~~ General setup -> Kernel compression mode (ZSTD)
~~ General setup -> Compiler optimization level (Optimize for size (-Os))
make -j12 deb-pkg
dpkg -i ../linux-image-*.deb ../linux-headers-*.deb
make clean
git reset --hard HEAD

4.
echo "deb [allow-insecure=yes] http://repo.mysql.com/apt/debian/ buster mysql-5.7 mysql-apt-config mysql-tools-preview mysql-tools" >> /etc/apt/sources.list

dpkg --add-architecture i386
apt update

apt install gcc make git gcc-multilib zlib1g zlib1g-dev libc6-dev mysql-community-server mysql-community-client mysql-common pcre2-utils:i386 libmariadb-dev:i386 libmariadb-dev-compat:i386
apt install libpcre3*:i386

### Debian 13
mysql only based
apt -y install libpcre2-dev:i386
ls -s /lib/i386-linux-gnu/libpcre2-32.so /lib/libpcre.so
