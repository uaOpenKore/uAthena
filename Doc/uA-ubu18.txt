systemctl isolate multi-user.target
systemctl set-default multi-user.target

apt -y install ssh mc
mcedit /etc/ssh/sshd_config
systemctl start ssh
systemctl enable ssh


apt -y remove plymo* libreoff*

apt -y install curl git libncurses5-dev gcc make autoconf exuberant-ctags bc libssl-dev

dpkg --add-architecture i386
apt update

echo "deb [allow-insecure=yes] http://repo.mysql.com/apt/ubuntu/ bionic mysql-5.7 mysql-apt-config mysql-tools-preview mysql-tools" >> /etc/apt/sources.list

apt -y install gcc-multilib zlib1g zlib1g-dev libc6-dev mysql-common pcre2-utils:i386 zlib1g:i386 zlib1g-dev:i386 libc6-dev:i386
apt -y install libpcre3:i386
apt -y install  mysql-community-client mysql-community-server libmysqlclient-dev:i386
apt -y upgrade

ln -s /lib/i386-linux-gnu/libpcre.so.3 /lib/libpcre.so

reboot
